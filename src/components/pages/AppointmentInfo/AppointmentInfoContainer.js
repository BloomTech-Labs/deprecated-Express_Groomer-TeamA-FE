import React from 'react';
import RenderAppointmentInfo from './RenderAppointmentInfo';

function AppointmentInfoContainer({ LoadingComponent }) {
  return (
    <>
      <RenderAppointmentInfo />
    </>
  );
}

export default AppointmentInfoContainer;
